# New-session prompt

Use the connected Figma integration to continue the existing native Figma Design file named **TrackHire Pixel Perfect Redesign**:

`https://www.figma.com/design/nyFC2u7V7yRwBMr6y9MDOc`

The complete approved visual reference set is located at:

`/home/ubuntu/code/ats-resume-tracker-ai/design-references/trackhire-approved`

Read `README.md` in that folder before creating anything. It defines the reference hierarchy and conflict-resolution rules. Use `01-landing-desktop-primary.png` as the primary desktop landing-page target, while `02-header-borderless-precedence.png` overrides the header shown in other concepts.

Then read `design-system-state-trackhire-redesign-20260813.json`. It contains the existing Figma file key, collection IDs, style IDs, completed steps, and remaining work. Inspect and reuse the existing 4 collections, 97 variables, and 6 text styles. **Do not create another Figma file and do not recreate those foundations.** Continue from `P1.f-partial`, completing the remaining styles only if required, followed by reusable components, screen construction, and screenshot-based validation.

Reproduce the approved screens precisely using editable native Figma layers, Auto Layout, semantic color variables, text styles, and reusable components. Do not reinterpret or average the concepts. Validate every major screen by comparing a Figma screenshot directly with its named reference and correct discrepancies before continuing.

When complete, return the Figma file URL and list any unavoidable font substitutions or visual differences.
